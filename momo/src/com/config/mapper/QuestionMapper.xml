<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.momo.QuestionMapper">
	
	<!-- 조회수 얻기 -->
	<select id="getReadCnt" resultType="int" parameterType="int">
		select readCnt from question
		where qnum = #{qnum}
	</select>
	<!-- 조회수 증가 -->
	<update id="readCntPlus" parameterType="int">
		update question
		set readCnt = readCnt +1
		where qnum =#{qnum}
	</update>
	
	<!-- 게시글 정보 등록 -->
	<insert id="insertQuestion" parameterType="QuestionDTO">
		insert into question(qnum,id,reple,category,password,title,content,author)
		values(#{qnum}+1,#{id},#{reple},#{category},#{password},#{title},#{content},#{author})
	</insert>
	
	<!-- 상세보기 -->
	<select id="questionDetail" resultType="QuestionDTO" parameterType="int">
		select qnum,reple,password,category,title,content,author,to_char(writeday,'yyyy-MM-dd') as "writeday",readCnt
		from question
		where qnum =#{qnum}
	</select>
	
	<!-- 리스트 개수 세는 쿼리 -->
	<select id="questionTotalRecord" resultType="int" parameterType="HashMap">
		select count(*) from question 
		<where>
			<choose>
				<when test="searchType =='title'">
					title like '%'||#{searchValue}||'%'
				</when>
				<when test="searchType =='content'">
					content like '%'||#{searchValue}||'%'
				</when>
				<when test="searchType =='author'">
					author like '%'||#{searchValue}||'%'
				</when>
				<when test="searchType =='category'">
					category = #{searchValue}
				</when>
			</choose>
		</where>
	</select>
	
	<!-- 리스트 가져오는 쿼리 -->
	<select id="questionList" resultType="QuestionDTO" parameterType="HashMap">
		select qnum,id,reple,password,category,title,content,author,to_char(writeday,'yyyy-MM-dd') as "writeday",readCnt
		from question
		order by qnum desc
	</select>
</mapper>